import 'package:flutter/flutter.dart';

class LandingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold((
      appBar: AppBar(
        title: "Farmer Market Home",
      ),
      body: Center(
        child: Text("Landing Page here!"),
      ),
    );
  }
}
